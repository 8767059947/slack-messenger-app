<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Slack Messenger</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-900 text-white min-h-screen flex items-center justify-center px-4">
    <div class="w-full max-w-xl bg-gray-800 p-8 rounded-2xl shadow-lg">
      <h1 class="text-3xl font-bold text-center mb-6 text-teal-400">Slack Messenger</h1>
      <form action="/send" method="POST" class="space-y-6">
        <div>
          <label for="message" class="block text-lg font-medium mb-2 text-gray-200">Message: </label>
          <textarea
            name="message"
            id="message"
            rows="5"
            placeholder="Enter your message here..."
            class="w-full p-4 bg-gray-700 text-white rounded-xl border border-gray-600 focus:outline-none focus:ring-2 focus:ring-teal-400 resize-none"
            required
          ></textarea>
        </div>
        <div class="text-center">
          <button
            type="submit"
            class="bg-teal-500 hover:bg-teal-400 text-white font-semibold py-2 px-6 rounded-xl transition duration-300 shadow-md"
          >
            🚀 Send Message
          </button>
        </div>
      </form>
    </div>
  </body>
</html>
